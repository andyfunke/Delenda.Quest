"use client";

import { CONCEPTS } from "./concepts";
import { fmt, type GameState, type Maneuver } from "./game";

function Concept({id,children}:{id:string;children:React.ReactNode}){const c=CONCEPTS[id];return <span className="term" tabIndex={0}>{children}<span className="term-tip"><b>{c.label}</b><span>{c.definition}</span>{c.normal&&<em>NORMAL // {c.normal}</em>}<strong>CONSEQUENCE // {c.consequence}</strong><a href={`?wiki=${id}`} target="_blank" rel="noreferrer">OPEN WIKI ARTICLE ↗</a></span></span>}

export function AdversaryPanel({s,m}:{s:GameState;m:Maneuver}){const l=s.adversaryLedger;const uncertainty=(100-s.intelligence)/160;const low=l?.estimateLow??Math.round(s.enemy*(1-uncertainty)),high=l?.estimateHigh??Math.round(s.enemy*(1+uncertainty));const observed=l?.observedOrders??[];const adaptation=s.adversary?.adaptation[m.id]??0;return <section className="adversary-panel"><header><div><small>ENEMY SYSTEM // INTELLIGENCE ASSESSMENT</small><b>{s.adversary?.objective?.toUpperCase()??"OBJECTIVE UNCLASSIFIED"}</b></div><div><small>ASSESSED FORCE</small><b>{fmt(low,true)}–{fmt(high,true)}</b></div><div><small>INTELLIGENCE CONFIDENCE</small><b>{s.intelligence.toFixed(0)}%</b></div><div><small>ADAPTATION TO THIS MANEUVER</small><b>{adaptation} / 8</b></div></header><div className="adversary-body"><div><Concept id="enemy-orders">OBSERVED ORDERS</Concept>{observed.length?<ul>{observed.map(x=><li key={x}>{x}</li>)}{l?.hiddenOrders?<li className="hidden-order">{l.hiddenOrders} ORDER{l.hiddenOrders===1?"":"S"} UNCLASSIFIED</li>:null}</ul>:<p>No current-day orders have resolved. The opening estimate rests on prewar signatures.</p>}</div><div className="adversary-metrics"><div><Concept id="enemy-posture">LAST POSTURE</Concept><b>{l?.posture??s.adversary.posture}</b><small>{l?`${l.pressure.toFixed(2)} KM pressure imposed`:"assessment only"}</small></div><div><Concept id="enemy-production">PRODUCTION SIGNATURE</Concept><b>{l?.productionTarget??s.adversary.productionTarget}</b><small>{l?`${fmt(l.munitionsOutput,true)} output // ${fmt(l.munitionsUse,true)} use`:"output not yet observed"}</small></div><div><Concept id="enemy-countermeasure">COUNTERMEASURE</Concept><b>{l&&l.observedOrders.length===3?l.countermeasure:"UNCONFIRMED"}</b><small>classification requires 65 Intelligence</small></div><div><Concept id="enemy-doctrine">DOCTRINE</Concept><b>{s.adversary.doctrine} OBSERVATION</b><small>changes as enemy methods survive contact</small></div></div></div><footer>REPETITION IS EVIDENCE // EACH REPEATED MANEUVER CAN ADD 1.5% ENEMY ADAPTATION PENALTY, MAXIMUM 12%</footer></section>}
