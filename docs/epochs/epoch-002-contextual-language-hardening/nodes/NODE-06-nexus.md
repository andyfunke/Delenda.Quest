# NODE-06 — Nexus vertical slice and mutation proof

## Output

The existing Nexus context boundary was verified as the single production
compiler route. It builds the disclosed contextual language once, passes it to
`compileAvaCommand`, and carries the result through the typed request IR and
terminal realization. No browser, terminal, SSH, or MCP parser was added.

## Live corpus

The real Nexus corpus covers territory/advance advice, front and kilometer
explanations, adversary/condition/loss reports, objective explanation, and
strategy advice. Every result is exact, typed, and state-bound.

## Mutation proof

The test snapshots state, actions, day, decisions, prepared orders, and the
session plan for every contextual read. Consequential prefixes, negation,
confirmation, and front resolution remain outside contextual advice.

## Verification

- Full live corpus and negative-neighbor tests pass.
- Web and native terminal/SSH parity remains covered by the existing suite.
- No HTTP SSH path, production deployment, D1 write, or shadow mutation.
