# Epoch 017 — Deterministic self-training and meta-rule proposal

Status: **COMPLETE**

**Depends on:** Epoch 015 + exported corpus (013/014). Epoch 016 optional.

Trainer uses §4.5 config only. Independent evaluator cannot import trainer.
Prism proposals emit `PROPOSED` only.
