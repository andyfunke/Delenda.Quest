# NODE-23 — Compose typed Ava advice through canonical calculus

Status: implementation complete; immutable receipt follows the implementation commit.

Implementation commit: `0656460`

Contract:

- Advice is derived from the active compiled decision winner and ranking.
- Objective, target, sector, disclosed situation bands, and query criteria remain typed inputs with explicit provenance.
- Equations, rules, alternatives, uncertainties, and unavailable coupled-order evidence are references to the canonical calculus, not recomputed values.
- Recommendation authority is `COMPILED_DECISION_WINNER`; no order is prepared or issued.
- Read-only and negated/consequential command boundaries remain owned by the existing compiler and Nexus.

Acceptance evidence:

- repeated advice requests are byte-stable and leave `GameState` unchanged;
- the recommendation is one visible canonical winner and is tied to the calculus ranking;
- priority axes and objective context are present even when no contextual priority phrase is used;
- no hidden, private, random, sealed, or mutation field is emitted.

Next handoff: NODE-24 / compile bounded typed maneuver comparisons.
