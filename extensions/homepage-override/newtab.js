// MV3 CSP forbids inline scripts, so the redirect lives here.
// replace() keeps the empty new-tab page out of history, so Back
// from Perplexity doesn't bounce you forward again.
location.replace("https://www.perplexity.ai/");
